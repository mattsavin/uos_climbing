import './style.css';
import { initApp } from './main';

// Initialize core app functionalities (Navbar, Footer, etc.)
initApp();

document.addEventListener('DOMContentLoaded', () => {
    console.log("Beginner's Guide page initialized");
});
